import Toast from '~/helpers/Toast'

export default function ({ $cookies, redirect, $route }) {
    // if ($cookies.get('slate-token') === undefined || !$route.query.token) {
    //     Toast(' ', 'You must be logged in to access this page', 'warning')
    //   redirect('https://www.slate.ng/auth/login?publisher=true')
    // }
  }